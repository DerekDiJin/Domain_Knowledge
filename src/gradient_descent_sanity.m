%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Sanity check: if there are values in f exceed the bounds, 
% return the boundary values; otherwise return f_in.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

function [ f_out ] = gradient_descent_sanity( f_in )
    if(sum(f_in<0) == )


end

